<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>About - CineRate</title>
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%);
      color: #fff;
      min-height: 100vh;
      margin: 0;
    }

    nav {
      background: rgba(0,0,0,0.8);
      padding: 1rem 0;
      box-shadow: 0 4px 6px rgba(0,0,0,0.3);
    }

    nav .container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 20px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    nav .logo { color: #e94560; font-weight:700; font-size:1.4rem; }
    nav ul { list-style:none; display:flex; gap:1.5rem; }
    nav a { color:#fff; text-decoration:none }
    nav a:hover { color:#e94560 }

    .hero {
      max-width: 1200px;
      margin: 3rem auto 1rem;
      padding: 0 20px;
      text-align: center;
    }

    .hero h1 {
      font-size: 2.4rem;
      background: linear-gradient(135deg,#e94560,#ff6b6b);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      margin-bottom: 0.5rem;
    }

    .hero p { color:#bbb; margin-bottom: 1.5rem }

    .content {
      max-width: 900px;
      margin: 0 auto 3rem;
      background: rgba(255,255,255,0.03);
      border: 1px solid rgba(233,69,96,0.12);
      padding: 24px;
      border-radius: 10px;
      color: #ddd;
    }

    .team {
      display: grid;
      grid-template-columns: repeat(auto-fit,minmax(180px,1fr));
      gap: 1rem;
      margin-top: 1.5rem;
    }

    .member {
      background: rgba(255,255,255,0.02);
      padding: 12px;
      border-radius: 8px;
      text-align: center;
      border: 1px solid rgba(233,69,96,0.06);
    }

    .member h4 { color:#fff; margin-bottom:6px }
    .member p { color:#bbb; font-size:0.9rem }

    footer { text-align:center; padding:2rem; color:#bbb; border-top:1px solid rgba(233,69,96,0.08) }
  </style>
</head>
<body>
  <nav>
    <div class="container">
      <div class="logo">🎬 CineRate</div>
      <ul>
        <li><a href="/home">Home</a></li>
        <li><a href="/movies">Movies</a></li>
        <li><a href="/about">About</a></li>
      </ul>
    </div>
  </nav>

  <section class="hero">
    <h1>About CineRate</h1>
    <p>Your friendly movie rating app — discover, rate and share.</p>
  </section>

  <div class="content">
    <h2>What we do</h2>
    <p>
      CineRate is a prototype lightweight movie rating platform built for quick feedback and discovery. Users can browse movies,
      submit ratings and reviews, and explore recommendations curated from community feedback.
    </p>

    <h3 style="margin-top:1rem">Key Features</h3>
    <ul>
      <li>Rate movies with a simple 1–5 star system</li>
      <li>Save reviews and view popular titles</li>
      <li>Mobile-friendly, responsive UI</li>
    </ul>
  </div>

  <footer>
    <p>&copy; <%= new Date().getFullYear() %> CineRate — Demo Website</p>
  </footer>
</body>
</html>
