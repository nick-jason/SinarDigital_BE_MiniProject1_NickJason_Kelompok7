<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CineRate - Movie Rating App</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%);
            color: #fff;
            min-height: 100vh;
        }

        /* Navigation */
        nav {
            background: rgba(0, 0, 0, 0.8);
            padding: 1rem 0;
            position: sticky;
            top: 0;
            z-index: 100;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.3);
        }

        nav .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        nav .logo {
            font-size: 1.5rem;
            font-weight: bold;
            color: #e94560;
        }

        nav ul {
            list-style: none;
            display: flex;
            gap: 2rem;
        }

        nav a {
            color: #fff;
            text-decoration: none;
            transition: color 0.3s;
        }

        nav a:hover {
            color: #e94560;
        }

        /* Hero Section */
        .hero {
            max-width: 1200px;
            margin: 0 auto;
            padding: 80px 20px;
            text-align: center;
        }

        .hero h1 {
            font-size: 3.5rem;
            margin-bottom: 1rem;
            background: linear-gradient(135deg, #e94560, #ff6b6b);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .hero p {
            font-size: 1.2rem;
            color: #bbb;
            margin-bottom: 2rem;
        }

        .cta-buttons {
            display: flex;
            gap: 1rem;
            justify-content: center;
            flex-wrap: wrap;
        }

        .btn {
            padding: 12px 30px;
            border: none;
            border-radius: 5px;
            font-size: 1rem;
            cursor: pointer;
            transition: all 0.3s;
            text-decoration: none;
            display: inline-block;
        }

        .btn-primary {
            background: linear-gradient(135deg, #e94560, #ff6b6b);
            color: white;
        }

        .btn-primary:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 15px rgba(233, 69, 96, 0.4);
        }

        .btn-secondary {
            background: transparent;
            border: 2px solid #e94560;
            color: #e94560;
        }

        .btn-secondary:hover {
            background: #e94560;
            color: white;
        }

        /* Features Section */
        .features {
            max-width: 1200px;
            margin: 4rem auto;
            padding: 0 20px;
        }

        .features h2 {
            text-align: center;
            font-size: 2.5rem;
            margin-bottom: 3rem;
        }

        .feature-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 2rem;
        }

        .feature-card {
            background: rgba(255, 255, 255, 0.05);
            padding: 2rem;
            border-radius: 10px;
            border: 1px solid rgba(233, 69, 96, 0.3);
            transition: all 0.3s;
            text-align: center;
        }

        .feature-card:hover {
            background: rgba(233, 69, 96, 0.1);
            border-color: #e94560;
            transform: translateY(-5px);
        }

        .feature-icon {
            font-size: 3rem;
            margin-bottom: 1rem;
        }

        .feature-card h3 {
            font-size: 1.5rem;
            margin-bottom: 1rem;
            color: #e94560;
        }

        .feature-card p {
            color: #bbb;
            line-height: 1.6;
        }

        /* Popular Movies Section */
        .popular-movies {
            max-width: 1200px;
            margin: 4rem auto;
            padding: 0 20px;
        }

        .popular-movies h2 {
            text-align: center;
            font-size: 2.5rem;
            margin-bottom: 3rem;
        }

        .movie-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 2rem;
        }

        .movie-card {
            background: rgba(255, 255, 255, 0.05);
            border-radius: 10px;
            overflow: hidden;
            border: 1px solid rgba(233, 69, 96, 0.3);
            transition: all 0.3s;
            cursor: pointer;
        }

        .movie-card:hover {
            transform: scale(1.05);
            border-color: #e94560;
        }

        .movie-poster {
            width: 100%;
            height: 250px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 3rem;
        }

        .movie-info {
            padding: 1.5rem;
        }

        .movie-title {
            font-size: 1.1rem;
            font-weight: bold;
            margin-bottom: 0.5rem;
        }

        .movie-rating {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            margin-bottom: 0.5rem;
        }

        .stars {
            color: #e94560;
        }

        .rating-score {
            font-weight: bold;
        }

        .movie-genre {
            font-size: 0.9rem;
            color: #bbb;
        }

        /* Footer */
        footer {
            background: rgba(0, 0, 0, 0.8);
            text-align: center;
            padding: 2rem;
            margin-top: 4rem;
            border-top: 1px solid rgba(233, 69, 96, 0.3);
        }

        footer p {
            color: #bbb;
        }

        @media (max-width: 768px) {
            .hero h1 {
                font-size: 2rem;
            }

            .cta-buttons {
                flex-direction: column;
            }

            .btn {
                width: 100%;
            }
        }
    </style>
</head>
<body>
    <!-- Navigation -->
    <nav>
        <div class="container">
            <div class="logo">🎬 CineRate</div>
            <ul>
                <li><a href="/home">Home</a></li>
                <li><a href="/movies">Movies</a></li>
                <li><a href="/about">About</a></li>
            </ul>
        </div>
    </nav>

    <!-- Hero Section -->
    <section class="hero">
        <h1>Welcome to CineRate</h1>
        <p>Discover, Rate, and Share Your Favorite Movies with Friends</p>
        <div class="cta-buttons">
            <a href="/product" class="btn btn-primary">Browse Movies</a>
            <a href="/submission" class="btn btn-secondary">Rate a Movie</a>
        </div>
    </section>

    <!-- Features Section -->
    <section class="features">
        <h2>Why Choose CineRate?</h2>
        <div class="feature-grid">
            <div class="feature-card">
                <div class="feature-icon">⭐</div>
                <h3>Easy Ratings</h3>
                <p>Rate movies with a simple and intuitive interface. Share your honest opinions with the community.</p>
            </div>
            <div class="feature-card">
                <div class="feature-icon">🎥</div>
                <h3>Vast Collection</h3>
                <p>Access a massive database of movies from all genres and eras. Find hidden gems and classics.</p>
            </div>
            <div class="feature-card">
                <div class="feature-icon">👥</div>
                <h3>Community Driven</h3>
                <p>Connect with fellow movie enthusiasts. Read reviews and discover what others love.</p>
            </div>
            <div class="feature-card">
                <div class="feature-icon">📊</div>
                <h3>Smart Recommendations</h3>
                <p>Get personalized movie recommendations based on your ratings and preferences.</p>
            </div>
            <div class="feature-card">
                <div class="feature-icon">🔐</div>
                <h3>Secure & Private</h3>
                <p>Your ratings and preferences are safe with us. Complete privacy protection.</p>
            </div>
            <div class="feature-card">
                <div class="feature-icon">📱</div>
                <h3>Mobile Friendly</h3>
                <p>Rate and browse movies anytime, anywhere. Fully responsive design.</p>
            </div>
        </div>
    </section>

    <!-- Popular Movies Section -->
    <section class="popular-movies">
        <h2>Recent Movies on Showing</h2>
        <div class="movie-grid">
            <% if (movies && movies.length > 0) { %>
                <% movies.forEach((movie) => { %>
                    <div class="movie-card">
                        <div class="movie-poster">🎬</div>
                        <div class="movie-info">
                            <div class="movie-title"><%= movie.moviename || 'Unknown' %></div>
                            <div class="movie-rating">
                                <span class="stars">★★★★★</span>
                                <span class="rating-score"><%= movie.rating || 'N/A' %></span>
                            </div>
                            <div class="movie-genre"><%= movie.genre || 'Drama' %></div>
                        </div>
                    </div>
                <% }); %>
            <% } else { %>
                <p style="grid-column: 1/-1; text-align: center; color: #bbb;">No movies available</p>
            <% } %>
        </div>
    </section>

    <!-- Footer -->
    <footer>
        <p>&copy; 2024 CineRate. All rights reserved. | Your favorite movie rating platform</p>
    </footer>
</body>
</html>
